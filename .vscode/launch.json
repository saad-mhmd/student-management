{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Karma Tests",
            "url": "http://localhost:9876",
            "webRoot": "${workspaceFolder}",
            "runtimeArgs": ["--headless", "--disable-gpu"]
        }
    ]
}
